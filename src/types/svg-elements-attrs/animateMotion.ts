import type {
  Accumulate,
  Additive,
  Begin,
  By,
  CalcMode,
} from "../attrs/SVGAttributes";

export type SVGAnimateMotionElementAttributes = {
  accumulate: Accumulate;
  additive: Additive;
  begin: Begin;
  by: By;
  calcMode: CalcMode;
};
