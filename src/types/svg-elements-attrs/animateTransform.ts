import type {
  Accumulate,
  Additive,
  AttributeName,
  Begin,
  By,
  CalcMode,
} from "../attrs/SVGAttributes";

export type SVGAnimateTransformElementAttributes = {
  accumulate: Accumulate;
  additive: Additive;
  attributeName: AttributeName;
  begin: Begin;
  by: By;
  calcMode: CalcMode;
};
